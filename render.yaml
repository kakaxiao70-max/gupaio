services:
  - type: web
    name: gupiao-streamlit
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python api_server.py
    envVars:
      - key: DEEPSEEK_API_KEY
        sync: false
